import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SeriesScreen = (props) => (
    <View>
        <Text>Esta é a SeriesScreen</Text>
    </View>
);

const styles = StyleSheet.create({

});

export default SeriesScreen;